# FDA Food Safety Data Pipeline

To run the first part, run `node '.\Part 1\part1.js'` from the command line.
To change the concurrency limit from the default of 3, put it as an argument.
For example, `node '.\Part 1\part1.js' 5` changes the concurrency limit to 5.